/**
 * @type IUserData
 * @param username: {string} Nombre del usuario unico
 * @param email: {string} Correo del usuario unico
 */
export type IUserData = {
    username: string;
    email: string;
}